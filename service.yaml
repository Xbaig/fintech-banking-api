apiVersion: v1
kind: Service
metadata:
  name: fintech-banking-api-service
spec:
  type: LoadBalancer
  selector:
    app: fintech-banking-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000